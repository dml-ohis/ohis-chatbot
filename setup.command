#!/usr/bin/env bash
# ============================================================
# 10x Analyst — macOS Double-Click Launcher
# Double-click this file in Finder to install & run
# ============================================================

# Navigate to the script's directory (so it works from Finder double-click)
cd "$(dirname "$0")"

# Run the main setup script
bash setup.sh
